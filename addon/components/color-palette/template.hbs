{{#power-select
  selected=selected
  options=colors
  searchEnabled=false
  renderInPlace=renderInPlace
  selectedItemComponent='ember-palette-selected'
  dropdownClass='ember-palette-dropdown'
  onchange=(action onselect) as |color|}}
  {{ember-palette-selected option=color class='color-item'}}
{{/power-select}}
